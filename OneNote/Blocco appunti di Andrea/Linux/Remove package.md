To remove a package deb we can:
 
```
sudo apt-get remove <package name>


sudo yum remove <package name>

```

```
apt-get remove <package name>
```
 
To remove packages based on RPM system:
 
```
sudo yum remove <package name>
```